{% extends 'base.html.twig' %}

{% block title %}Snowtricks -{{ 'home' | trans }}{% endblock %}

{% block body %}
<section class="jumbotron jumbotron-fluid vh-100"  style="background-image: url({{ absolute_url(asset('build/images/background.jpg')) }});">
    <div class="container text-center">
        <div class="row">
            <div class="col mt-5 pt-5">
                <h1 class="display-6 text-white">SnowTricks</h1>
                <p class="lead text-white">{{ 'lead' | trans }}</p>
            </div>
        </div>
    </div>
</section>
{{ render(path('app_trick_index', {max: 10})) }}
{% endblock %}
