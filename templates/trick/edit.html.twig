{% extends 'base.html.twig' %}

{% block title %}{{ 'edit' | trans }} {{ 'trick' | trans }}{% endblock %}

{% block body %}
    <section class="background-layout-fixed">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="card shadow-lg rounded border-0 my-5 p-5">
                        <div class="row justify-content-between mb-2">
                            <div class="col">
                                <a role="button" class="btn btn-dark text-decoration-none" href="{{ path('app_home') }}"><i class="bi bi-arrow-left"></i></a>
                            </div>
                            <div class="col text-end">
                                {{ include('trick/_delete_form.html.twig') }}
                            </div>
                        </div>
                        <h1 class="card-title text-center">{{ 'edit' | trans }} {{ 'trick' | trans }}</h1>
                        <div class="card-body">
                            {{ include('trick/_form.html.twig', {'button_label': 'Update'}) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
