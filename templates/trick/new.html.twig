{% extends 'base.html.twig' %}

{% block title %}{{ 'create_new' | trans }} {{ 'trick' | trans }}{% endblock %}

{% block body %}
    <section class="container vh-75 my-1">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <h1 class="text-center">{{ 'create_new' | trans }} {{ 'trick' | trans }}</h1>
                {{ include('trick/_form.html.twig') }}
            </div>
        </div>
        <div class="text-center">
            <a href="{{ path('app_home') }}" role="button" class="btn btn-secondary">{{ 'back_to_list' | trans }}</a>
        </div>
    </section>
{% endblock %}
