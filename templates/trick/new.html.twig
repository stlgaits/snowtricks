{% extends 'base.html.twig' %}

{% block title %}{{ 'create_new' | trans }} {{ 'trick' | trans }}{% endblock %}

{% block body %}

    <section class="background-layout-fixed">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8 my-4">
                    <div class="card shadow p-5">
                        <div>
                            <a href="{{ path('app_home') }}" role="button" class="btn btn-dark"><i class="bi bi-arrow-left"></i></a>
                        </div>
                        <div class="row justify-content-center">
                            <div class="col-md-8">
                                <h1 class="text-center">{{ 'create_new' | trans }} {{ 'trick' | trans }}</h1>
                                {{ include('trick/_form.html.twig') }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
